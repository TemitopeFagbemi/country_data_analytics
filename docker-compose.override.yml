# services:
#   minio:
#     image: minio/minio:RELEASE.2025-07-23T15-54-02Z
#     container_name: minio
#     environment:
#       MINIO_ROOT_USER: adminuser
#       MINIO_ROOT_PASSWORD: StrongPassword123
#       MINIO_ROOT_USER_FILE: ""
#       MINIO_ROOT_PASSWORD_FILE: ""
#       BUCKET_NAME: countries-bucket
#     ports:
#       - "9000:9000"
#       - "9101:9101"
#     restart: unless-stopped
#     command: server /data --console-address ":9101"   # Start MinIO server and console on specified ports
#     env_file:
#       - .env

#   scheduler:
#     env_file:
#       - .env

#   api-server:
#     env_file:
#       - .env

#   dag-processor:
#     env_file:
#       - .env

#   triggerer:
#     env_file:
#       - .env

#     volumes:
#       - minio_data:/data
#       - minio_config:/root/.minio
#     networks:
#       - default

# volumes:
#   minio_data:
#   minio_config:

# # networks:
# #       - default


services:
  minio:
    image: minio/minio:RELEASE.2025-07-23T15-54-02Z
    container_name: minio
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"
      - "9101:9001"
    environment:
      MINIO_ROOT_USER: adminuser
      MINIO_ROOT_PASSWORD: StrongPassword123
    volumes:
      - minio_data:/data
      - ./country_dbt:/usr/local/airflow/country_dbt

volumes:
  minio_data: